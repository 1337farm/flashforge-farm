#include <render/Program.hpp>

#ifndef FLASHFORGE_FARM_NATIVE_HPP
#define FLASHFORGE_FARM_NATIVE_HPP

// get_current_shader() is declared generically by the engine's render/Program.hpp
// (included above) and DEFINED in farm_native.cpp. The engine's utility render
// code calls it to get the app-layer's currently bound GL shader program.

#endif //FLASHFORGE_FARM_NATIVE_HPP
